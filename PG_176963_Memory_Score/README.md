# [176963] [추억 점수]

- **Python**
  - [x] 1차 (O)
- **Javascript**
  - [] 1차

### 🔗 문제 링크

[문제 링크](https://school.programmers.co.kr/learn/courses/30/lessons/176963)

### ✨ 풀이 전략 / 핵심 아이디어

(여기에 문제 풀이 전략, 사용한 알고리즘, 자료구조 등을 작성하세요. 예를 들어...)

### ⏱️ 시간 복잡도 및 공간 복잡도

### 💭 회고 / 어려웠던 점

(이 문제를 풀면서 겪었던 시행착오, 새로 알게 된 점, 개선할 부분 등 개인적인 생각을 자유롭게 작성하세요.)

- 다양한 딕셔너리 생성 방법 연습하기
  ## zip 함수
      1. zip은 인자로 전달된 리스트 or 튜플 or 문자열을 조합해서 다수의 튜플을 만들어 내는데, 같은 위치에 있는 값들끼리 조합해서 튜플을 만들어 낸다.
      2. zip이 반환하는 객체는 iterable 객체이다.
      3. 따라서, tuple, dict, list에 담을 수 있다.
      4. 셋 이상의 값들을 조합해 내는 것도 가능하다.
  ```
  >>> d = list(zip(['a', 'b', 'c'], [1, 2, 3]))
  >>> d
  [('a', 1), ('b', 2), ('c', 3)]
  ```
  ```
  >>> d = dict(zip(['a', 'b', 'c'], [1, 2, 3]))
  >>> d
  {'a': 1, 'b': 2, 'c': 3}
  ```
  ```
  >>> c = list(zip('abc', (1, 2, 3), ['one', 'two', 'three']))
  >>> c
  [('a', 1, 'one'), ('b', 2, 'two'), ('c', 3, 'three')]
  ```
